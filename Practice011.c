//Program to find ASCII value of a character

#include <stdio.h>

int main()
{
    char c;
    printf("Enter character:");
    scanf("%c", &c);
    {
        printf("ASCII value of %c: %d", c,c);
    }
    return 0;
}